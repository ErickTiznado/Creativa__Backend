# .env.example
# IMPORTANTE: Copia este archivo a un archivo llamado '.env' y rellena los valores reales.

# ------------------------------------------------------------------
# Configuración de Servidor
# ------------------------------------------------------------------
# Puerto donde correrá el servidor (por defecto: 3000).
# Nota: hoy `app.js` escucha fijo en 3000, pero `src/config/index.js` sí lee PORT.
PORT=3000

# Entorno de ejecución (development | test | production)
NODE_ENV=development

# Clave secreta para firmar los Tokens JWT de Nicola Framework.
# Debe ser una cadena larga y compleja.
NICOLA_SECRET=tu_secreto_super_seguro_aqui

# ------------------------------------------------------------------
# Configuración de Base de Datos (Supabase)
# ------------------------------------------------------------------
# URL de conexión a tu proyecto de Supabase
SUPABASE_URL=https://tu-proyecto.supabase.co

# Llave anónima (anon public key) de Supabase.
# Se encuentra en Project Settings -> API.
#
# IMPORTANTE:
# - El código actual en `src/services/SupabaseClient.js` usa `SUPABASE_KEY`.
# - `src/config/index.js` acepta `SUPABASE_ANON_KEY` o `SUPABASE_KEY`.
SUPABASE_KEY=tu_supabase_anon_key_aqui

# Recomendado (alias explícito de la anon key). Si lo defines, el config lo prioriza.
SUPABASE_ANON_KEY=tu_supabase_anon_key_aqui

# Service Role Key (privada). Úsala SOLO en backend seguro.
SUPABASE_SERVICE_KEY=tu_supabase_service_role_key_aqui

# ------------------------------------------------------------------
# Google Cloud / Vertex AI / Storage
# ------------------------------------------------------------------
# Ruta al JSON del Service Account.
# Windows (ejemplo): C:\\Users\\TU_USUARIO\\keys\\gcp-sa.json
# Linux/Mac (ejemplo): /home/tu_usuario/keys/gcp-sa.json
GOOGLE_APPLICATION_CREDENTIALS=RUTA_ABSOLUTA_AL_JSON

# ID del proyecto de Google Cloud (Project ID, no el nombre visible)
GOOGLE_PROJECT_ID=tu-gcp-project-id

# Región de Vertex AI (por defecto: us-central1)
GOOGLE_LOCATION=us-central1

# Bucket de Google Cloud Storage donde se subirán/leerán assets (imágenes, etc.)
GCS_BUCKET_NAME=tu-bucket

# URL pública base del bucket (opcional). Si no se define, se infiere con storage.googleapis.com
GCS_PUBLIC_URL=https://storage.googleapis.com/tu-bucket